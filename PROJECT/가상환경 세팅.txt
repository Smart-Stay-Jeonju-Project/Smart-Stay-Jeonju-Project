# 가상환경 생성
# 시간 오래 걸려요....

방식 1. 가상 환경 생성을 environment.yml 파일 실행하여 만들기
# conda env create -n azen -f environment.yml



방식 2. 터미널에서 가상 환경 만들기

conda deactivate

-> 가상환경이 아무것도 실행되지 않고 있는 상태에서
conda create -n azen python=3.10 -y

conda activate azen

conda install -c conda-forge jpype1 pandas matplotlib openpyxl notebook scikit-learn nltk tqdm wordcloud -y

pip install flask jinja2 gunicorn pymysql beautifulsoup4 selenium webdriver-manager requests lxml html5lib konlpy

# 주피터에서 azen 커널 등록 (azen 선택된 상태에서 터미널에 입력)
python -m ipykernel install --user --name=azen